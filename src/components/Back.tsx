import React from 'react';
import { Link } from 'react-router-dom';

const Back = () => {
	return (
		<div>
			<button>
				<Link to="/">Back</Link>
			</button>
		</div>
	);
};

export default Back;
